# Панель модерации объявлений — тестовое задание Авито

Этот проект представляет собой упрощённую систему модерации объявлений в стиле Авито. Приложение включает список объявлений с фильтрами, детальную карточку объявления, возможность модерации и страницу статистики модератора.

---

## Функциональность

### ✔️ Список объявлений
- Отображение объявлений с ключевыми данными: название, цена, категория, статус, приоритет, дата.
- Разворачиваемые карточки объявлений.
- Быстрый переход к детальной странице.

### ✔️ Фильтры
- Фильтр по **статусу** (множественный выбор).
- Фильтр по **категории**.
- Фильтр по **диапазону цен** (слайдер).
- **Поиск** по названию объявления (с debounce).
- Кнопка **сброса всех фильтров**.

### ✔️ Детальная карточка объявления
Переход по маршруту `/item/:id`:
- фотографии объявления,
- описание,
- категория,
- цена,
- приоритет,
- продавец (имя, рейтинг),
- характеристики,
- история модерации.

### ✔️ Модерация
- Одобрение (`approve`)
- Отклонение (`reject`)
- Отправка на доработку (`revision`)
- Обновление истории модерации

### ✔️ Страница статистики `/stats`
- Карточки метрик:
    - количество проверенных объявлений,
    - процент одобрений,
    - процент отклонений,
    - среднее время модерации.
- Три графика:
    - активность по дням,
    - распределение решений,
    - статистика по категориям.
- Все запросы выполняются **параллельно** благодаря TanStack Query.

---

## 🧱 Использованные технологии

### Фронтенд
- **React 18**
- **TypeScript**
- **Vite**
- **React Router**
- **TanStack Query**
- **Tailwind CSS**
- **shadcn/ui**
- **lucide-react**, **@tabler/icons-react**

## 📝 Обоснование выбора технологий

### 💠 TanStack Query
Необязательная, но критически полезная библиотека:
- избавляет от ручной работы с `useEffect`,
- даёт кэширование,
- контроль ошибок,
- параллельные запросы,
- уменьшает количество багов.

Для проектов по типу панели модерации — это стандарт.

### 💠 shadcn/ui
Выбран потому что:
- быстро создаёт современный UI,
- компоненты легко кастомизировать,
- работает поверх Tailwind,
- выглядит аккуратно и знакомо.

### 💠 Tailwind CSS
Подходит для задач с большим количеством быстро меняющихся UI-элементов (фильтры, карточки, таблицы).  
Ускоряет разработку, особенно в тестовом задании.

### 💠 React Router
Нужен для маршрутов `/`, `/item/:id`, `/stats`.  
Работает стабильно, просто интегрируется.

---

## 🔧 Запуск проекта локально

### 1. Требования
- Node.js 16+
- npm или pnpm

---

### 2. Клонирование репозитория

```bash
git clone https://github.com/<your-username>/<repo>.git
cd <repo>
```

---

### 3. Запуск backend

```bash
cd tech-int3-server
npm install
npm run start
```

---

### 4. Запуск frontend

```bash
cd moderation-management-frontend
npm install
npm run dev
```

Открыть приложение:

```
http://localhost:5173
```

---

## 🚧 Известные недочёты проекта

Эти недочёты оставлены намеренно, чтобы показать понимание объёма работы и возможных улучшений.

- Обработка ошибок местами минимальная
- Многим компонентам нужно улучшить UX (анимации, skeleton).
- Типы надо, к сожалению, поправлять.
- Не реализовано сохранение фильтров между посещениями.
- Нет E2E/юнит тестов.
- Валидация минимальная.
- В некоторых местах структура может быть улучшена.

---

## Идеи для улучшения проекта

- Добавить сохранение фильтров в localStorage.
- Закончить адаптивность для мобильных устройств.
- Добавить тесты.
- Доработать skeleton-состояния страниц.
- Реализовать Docker для быстрого запуска.

---